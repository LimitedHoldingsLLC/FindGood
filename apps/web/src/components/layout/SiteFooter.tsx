export function SiteFooter() {
  return (
    <footer className="mt-20 border-t border-ink/10">
      <div className="mx-auto flex max-w-6xl flex-col gap-2 px-4 py-10 text-sm text-muted md:flex-row md:justify-between">
        <p>FindGood — good food, unusually good prices, near you.</p>
        <p>findgood.food</p>
      </div>
    </footer>
  );
}
